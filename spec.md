# 광림교회 청장년부 2026 영성수련회 등록 시스템 — 개발 스펙

이 문서는 Claude Code가 프로젝트를 처음부터 끝까지 구현할 때 참고하는 단일 소스(Single Source of Truth)입니다.
새 세션을 시작할 때마다 이 파일을 먼저 읽고, 아래 "구현 순서(Phase)"에 따라 단계적으로 진행합니다.

---

## 0. 프로젝트 개요

광림교회 청장년부 2026 영성수련회 참가 신청 및 관리 시스템.

- 사용자는 모바일/PC에서 참가 신청, 등록 확인/수정, QR 체크인을 할 수 있다.
- 관리자는 하나의 관리자 페이지에서 등록 현황, 입금 확인, 체크인 확인, 시스템 설정을 관리할 수 있다.
- GitHub Pages에 배포 가능한 정적 사이트 구조를 우선으로 하며, Firebase(Firestore)를 백엔드로 사용한다.

### 기술 스택

- React + TypeScript + Vite
- Tailwind CSS
- Firebase Firestore (데이터), Firebase Hosting 또는 GitHub Pages(정적 빌드)
- 컴포넌트 기반 설계, 데이터 계층(services/) 분리 — UI와 Firestore 접근 로직을 분리하여 향후 Supabase 등으로 교체 가능하게 설계

### 디자인 컨셉

- 푸르른 자연 느낌, 미니멀, 깨끗한 흰색 배경
- Green / Mint 계열 컬러
- 둥근 카드 UI (rounded-2xl 이상)
- 모바일 우선 반응형, PC 대응
- 심플한 애니메이션 (전환, 상태 변경 시 fade/slide 정도)
- 직관적인 UX, 큰 터치 영역

---

## 1. 정보 구조 (IA)

```
/ (메인 랜딩)
 ├─ 탭 1: 수련회 안내 및 등록
 └─ 탭 2: 등록확인 / 수정

/admin (관리자)
 ├─ 로그인 (비밀번호)
 ├─ 등록 현황
 ├─ 입금 확인
 ├─ 체크인 확인
 └─ 설정
```

---

## 2. 데이터 모델 (Firestore 컬렉션 설계 제안)

Claude Code는 아래 구조를 기본안으로 설계하되, 구현 단계에서 세부 필드명은 조정 가능합니다.

### `registrations` (등록자 = 가족 대표, 1 문서 = 1 가족)

```ts
{
  id: string;                 // 문서 ID = QR에 담기는 고유 ID
  name: string;
  affiliation: '청장1부' | '청장2부' | '지교회';
  phone: string;
  birthMonth: number;         // 1~12
  bloodType: 'A' | 'B' | 'O' | 'AB';
  lodging: 'O' | 'X';
  adults: {                   // 대표 본인 제외, 추가된 성인
    name: string;
    birthMonth: number;
    bloodType: 'A' | 'B' | 'O' | 'AB';
    lodging: 'O' | 'X';
  }[];
  children: {
    name: string;
    age: number;               // 만 나이
    lodging: 'O' | 'X';
  }[];
  note: string;                // 추가 전달사항
  totalCount: number;          // 대표 + adults + children, 자동계산
  amountDue: number;           // 자동계산 (설정값 기반)
  paymentStatus: '입금전' | '입금완료';
  checkedIn: boolean;
  checkedInAt: Timestamp | null;
  createdAt: Timestamp;
  updatedAt: Timestamp;
}
```

### `settings` (단일 문서, 예: `settings/config`)

```ts
{
  earlyBirdStart: string;      // ISO date
  earlyBirdEnd: string;
  regularStart: string;
  regularEnd: string;
  adultFee: number;
  childFee: number;
  bankName: string;
  accountNumber: string;
  accountHolder: string;
  earlyBirdBenefit: string;    // textarea
  notice: string;              // 공지사항 textarea
  guideText: string;           // 수련회 안내 본문 textarea
  registrationState: '등록예정' | '등록중' | '등록마감'; // 날짜보다 우선 적용
}
```

### 인원/금액 계산 규칙

- `totalCount` = 1(대표) + adults.length + children.length
- `amountDue` = (1 + adults.length) × `settings.adultFee` + children.length × `settings.childFee`
- 대표는 성인으로 간주하여 성인 등록비를 적용한다.

---

## 3. 사용자 화면

### 3.0 메인 랜딩 페이지

- 상단 중앙: "광림교회 청장년부 2026 영성수련회" 타이틀
- 동일 크기 버튼 2개: `[ 수련회 안내 및 등록 ] [ 등록확인/수정 ]`
- 기본 활성 탭: **수련회 안내**
- 우측 상단: 작은 텍스트/아이콘 링크 `관리자 페이지` → `/admin`으로 이동

### 3.1 탭 1 — 수련회 안내 및 등록

- 상단: `settings.notice` (공지사항)를 카드 형태로 표시. 여러 줄 지원.
- 얼리버드 등록 기간 중에는 `settings.earlyBirdBenefit`(얼리버드 혜택 안내)을 별도 카드로 표시하고, 기간 종료 시 자동 숨김.
- 본문: `settings.guideText`(수련회 안내) — 줄바꿈/번호목록/글머리기호를 지원하는 형태로 렌더링(마크다운 유사 텍스트 렌더 또는 `white-space: pre-wrap`으로 처리).
- 행사 시간표 영역: 카드 형태, 현재는 "추후 업데이트 예정입니다." Placeholder.

안내문: "가족은 대표 1명만 신청합니다."

**등록 상태 제어 (중요, 설정값 기반)**
1. `settings.registrationState`가 `'등록예정'`이면 신청 버튼 비활성화 + "등록 예정입니다." 표시 (날짜와 무관하게 최우선 적용)
2. `'등록마감'`이면 신청 버튼 비활성화 + "등록이 마감되었습니다." 표시 (최우선 적용)
3. `'등록중'`인 경우에만 날짜 로직 적용:
   - 오늘이 얼리버드 기간 내: 얼리버드 등록 중 배지 표시, 등록 가능
   - 오늘이 일반 등록 기간 내: 일반 등록 안내 표시, 등록 가능
   - 두 기간 모두 종료: 등록 불가 + "등록이 마감되었습니다."
   - 등록 시작 전: 등록 불가 + "등록 예정입니다."

**입력 항목 (대표자)**
- 이름 (text)
- 소속 (dropdown: 청장1부 / 청장2부 / 지교회)
- 연락처 (text)
- 생년월 (dropdown 1~12월)
- 혈액형 (dropdown: A/B/O/AB)
- 숙박 여부 (dropdown: O/X)

**가족 구성원 추가**
- "성인 추가" 버튼 → 카드 생성 (이름, 생년월, 혈액형, 숙박여부) — 여러 명 추가 가능, 카드별 삭제 가능하게 구현
- "자녀 추가" 버튼 → 카드 생성 (이름, 만 나이, 숙박여부) — 여러 명 추가 가능

**추가 전달사항**: textarea (placeholder 예시: "저녁에 도착합니다. / 첫날만 참석합니다. / 알레르기가 있습니다.")

**신청하기 버튼**
- Firestore에 `registrations` 문서 생성 (금액 자동 계산 포함)
- 성공 시 등록자 고유 ID를 로컬(localStorage)에 저장하고 **등록확인/수정** 탭으로 자동 이동

### 3.3 탭 3 — 등록확인 / 수정

로컬에 저장된 등록 ID 기준으로 조회 (ID가 없으면 이름+연락처로 조회하는 보조 수단 고려).

표시 항목:
- **등록 정보**: 등록자 정보, 가족 구성원(성인/자녀), 숙박 여부, 추가 전달사항
- **입금 안내**: `settings.bankName / accountNumber / accountHolder` 표시
  ```
  입금 계좌
  {은행명}
  {계좌번호}
  예금주 : {예금주}
  ```
- **총 입금 금액**: "총 참가 인원 : N명" + `amountDue` 표시 (설정값 기반 자동 계산)
- **입금 상태**: "아직 입금 전입니다." / "입금 완료되었습니다. 감사합니다."
- **버튼**: `등록 정보 수정`, `등록 취소`
- **QR 체크인 버튼**: 클릭 시 등록자 고유 ID를 인코딩한 QR 코드 생성 (예: `qrcode.react` 라이브러리 사용)
  - 관리자가 QR을 스캔 → 관리자 체크인 확인 화면으로 이동
  - 체크인 완료 시 이 화면에도 "체크인이 완료되었습니다." 상태 즉시 반영 (Firestore 실시간 구독 `onSnapshot` 권장)

---

## 4. 관리자 화면

### 4.0 로그인

- 우측 상단 링크로 진입
- 비밀번호: `3040` (MVP 단계에서는 하드코딩, 확장 시 관리자 비밀번호 변경 기능 고려)
- 인증 성공 시 세션 유지 30분 (sessionStorage + 타임스탬프로 구현), 만료 시 재로그인 요구

### 4.1 등록 현황

- 상단 큰 카드: "총 참가 인원 — 성인 00명 / 자녀 00명 / 총 00명" (등록자 + 모든 가족 구성원 합산, 등록자 수와 별개 지표)
- 검색: 등록자 이름
- 필터: 전체 / 청장1부 / 청장2부 / 지교회 → 적용 시 상단에 "성인 N명 / 자녀 N명" 즉시 갱신
- 표 컬럼: `분류 | 등록자 | 이름 | 소속 | 연락처 | 생년월 | 혈액형 | 숙박 | 입금 | 체크인 | 기타`
  - 가족 구성원은 등록자 바로 아래 행에 출력
  - 성인 행: 소속/연락처/입금여부/숙박여부는 등록자와 동일하게 자동 복사
  - 자녀 행: 생년월/혈액형 빈칸, 기타 컬럼에 "나이 : 만 N세" 표시
- 엑셀 다운로드: 전체 명단 (예: `xlsx` 또는 `sheetjs` 라이브러리 사용)

### 4.2 입금 확인

- 상단 통계: "총 등록자 N명 / 입금 완료 N명" (등록자 = 가족 대표 신청자 수, 가족 구성원 미포함)
- 금액 통계: "총액 / 입금 완료" 합계 금액
- 검색: 등록자 이름
- 필터: 소속 / 입금 여부
- 표 컬럼: `등록자 | 소속 | 등록 인원 | 등록비 | 입금 여부 | 등록자 정보 | 입금 확인`
  - 등록 인원 예: "성인 2명 / 자녀 1명"
  - `등록자 정보` 버튼 → 등록 현황 화면으로 이동 + 해당 이름으로 자동 검색
  - `입금 확인` 버튼 클릭 → 입금완료 처리, 버튼이 `취소하기`로 변경되어 되돌리기 가능

### 4.3 체크인 확인

- 관리자가 QR 스캔 (웹 카메라 기반 QR 리더 라이브러리, 예: `html5-qrcode` 고려)
- QR 인식 → 해당 등록자 정보 화면 오픈
  - 관리자 세션이 살아있으면 즉시 표시
  - 세션 만료 시 비밀번호(3040) 입력 후 자동 진행
- 표시 정보: 등록자, 가족 정보, 숙박 여부, 입금 여부, 전달사항
- `체크인 완료` 버튼 클릭 시:
  - 등록자 화면(3.3)에 "체크인이 완료되었습니다." 즉시 반영
  - 등록 현황 표의 체크인 컬럼이 "O"로 업데이트

### 4.4 설정

관리자만 접근, 저장 즉시 사용자 화면에 반영. 화면 구성 순서는 아래와 같다.

1. **얼리버드 등록 기간** — Date Picker 2개 (시작일/종료일)
2. **일반 등록 기간** — Date Picker 2개 (시작일/종료일)
3. **성인 등록비** — 숫자 입력, 단위 "원"
4. **자녀 등록비** — 숫자 입력, 단위 "원"
5. **입금 계좌**
   - 은행명 (text)
   - 계좌번호 (text)
   - 예금주 (text)
6. **얼리버드 혜택 안내** — textarea, 얼리버드 기간에만 노출, 종료 시 자동 숨김
7. **공지사항** — textarea, 여러 줄 지원, 수련회 안내 탭 상단 공지 카드로 표시
8. **수련회 안내** — textarea, 줄바꿈/번호목록/글머리기호 지원, 안내 탭 본문으로 사용
9. **등록 상태** — Radio/Toggle: `등록예정 / 등록중 / 등록마감`
   - 날짜 설정보다 **우선 적용**된다 (예: 일반 등록 기간 중이어도 관리자가 '등록마감' 선택 시 즉시 등록 중단)

**동작 규칙 요약**
| registrationState | 버튼 상태 | 문구 |
|---|---|---|
| 등록예정 | 비활성화 | "등록 예정입니다." |
| 등록중 (날짜 로직 적용) | 기간 내 활성화 | 얼리버드/일반 안내 |
| 등록마감 | 비활성화 | "등록이 마감되었습니다." |

하단 버튼:
- `완료` — 저장, "설정이 저장되었습니다." 메시지, 사용자/관리자 화면 즉시 반영
- `취소` — 저장하지 않고 마지막 저장 상태로 복원 (폼 상태를 로컬에서 초기값으로 리셋)

---

## 5. 공통 요구사항

- 모바일 우선 반응형 UI, PC 대응
- GitHub Pages 배포 가능한 정적 빌드 구조 유지 (환경변수로 Firebase config 분리)
- React + TypeScript + Vite
- Tailwind CSS
- 데이터 계층 분리 (`src/services/` 등에서 Firestore 접근을 캡슐화하여 향후 Supabase 전환 대비)
- 컴포넌트 기반 설계
- 관리자 화면은 대량 데이터에서도 빠른 검색/필터링 가능하도록 구현 (클라이언트 사이드 필터링으로 충분, 데이터 규모 커지면 페이지네이션 고려)

---

## 6. 향후 확장 기능 (이번 범위 아님, 참고용)

- 관리자 비밀번호 변경
- 관리자 계정 다중 권한
- 문자(SMS) 발송 / 카카오 알림톡
- 등록 마감 기능 고도화
- 참가비 자동 계산 규칙 세분화
- QR 재발급
- 체크인 시간 기록 상세화
- 참가 통계 대시보드
- 행사 시간표 관리 UI
- CSV/Excel 업로드

---

## 7. 구현 순서 제안 (Phase)

Claude Code 세션을 나눌 때 아래 순서를 권장합니다. 각 Phase는 별도 세션으로 진행 가능합니다.

1. **Phase 0 — 프로젝트 스캐폴딩**: Vite + React + TS + Tailwind 셋업, 라우팅(랜딩/관리자), Firebase 초기화
2. **Phase 1 — 데이터 계층 & 설정**: `settings` 컬렉션 CRUD, 관리자 설정 페이지 (4.4)부터 구현 — 다른 화면이 이 값에 의존하므로 우선 구현
3. **Phase 2 — 등록하기 (3.2)**: 폼 UI, 가족 구성원 추가/삭제, 등록 상태 로직, Firestore 저장
4. **Phase 3 — 등록확인/수정 (3.3)**: 조회, 수정, 취소, QR 생성
5. **Phase 4 — 수련회 안내 (3.1)**: 설정값 렌더링
6. **Phase 5 — 관리자: 등록 현황 (4.1)**: 표, 검색, 필터, 엑셀 다운로드
7. **Phase 6 — 관리자: 입금 확인 (4.2)**
8. **Phase 7 — 관리자: 체크인 확인 (4.3)**: QR 스캔 연동, 실시간 반영
9. **Phase 8 — 마무리**: 반응형 점검, 애니메이션, 에러 처리, GitHub Pages 배포 설정

각 Phase 시작 시 이 문서의 관련 섹션만 다시 확인하고, 이전 Phase에서 확정된 데이터 모델/설정 스키마를 변경하지 않는 것을 원칙으로 합니다.
